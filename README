# Separador de Áudio do YouTube

Um separador de áudio que extrai faixas de harmonia e percussão de vídeos do YouTube. Feito em Python, usa Librosa para o processamento e Tkinter para uma interface gráfica simples e amigável.

> **Nota:** Tire um print (screenshot) da sua aplicação rodando, salve o arquivo como `screenshot.png` na pasta raiz do projeto e o GitHub irá mostrá-lo aqui.

## ✨ Features

  * **Download Simples:** Baixa o áudio de qualquer vídeo do YouTube apenas com o link.
  * **Separação Inteligente:** Utiliza a biblioteca `librosa` para separar o áudio em duas faixas:
      * **Harmônica:** Contém vocais, melodias, baixo, etc.
      * **Percussiva:** Contém a bateria e outros elementos rítmicos.
  * **Interface Gráfica:** Uma janela simples e intuitiva criada com Tkinter, sem necessidade de usar o terminal para o processo principal.
  * **Feedback em Tempo Real:** A interface informa ao usuário o status do processo (download, processamento, salvamento).
  * **Exportação em `.wav`:** Salva os arquivos de áudio separados em formato WAV de alta qualidade.

## 🛠️ Tecnologias Utilizadas

  * **Python 3**
  * **Librosa:** Para análise e processamento de áudio.
  * **yt-dlp:** Para baixar o áudio do YouTube.
  * **Tkinter:** Para a construção da interface gráfica.
  * **FFmpeg:** Dependência externa para conversão de áudio.

## ⚙️ Instalação

Siga os passos abaixo para configurar e rodar o projeto em sua máquina local.

#### **1. Pré-requisitos**

  * **Python 3.8+:** Verifique se o Python está instalado (`python --version`).
  * **FFmpeg:** Essencial para a conversão de áudio. O FFmpeg precisa estar instalado e acessível no PATH do sistema.
      * [Instruções de instalação para Windows, macOS e Linux](https://ffmpeg.org/download.html).

#### **2. Clonar o Repositório**

```bash
git clone https://github.com/Mchristopher120/projetolibrosa.git
cd projetolibrosa
```

#### **3. Configurar o Ambiente Virtual e Instalar as Dependências**

É altamente recomendado usar um ambiente virtual para isolar as dependências do projeto.

```bash
# Criar o ambiente virtual
python -m venv venv

# Ativar o ambiente virtual
# Windows
.\venv\Scripts\activate
# macOS / Linux
source venv/bin/activate

# Instalar as bibliotecas a partir do arquivo requirements.txt
pip install -r requirements.txt
```

## 🚀 Como Usar

Com o ambiente configurado e as dependências instaladas, basta executar o script da interface gráfica.

1.  Certifique-se de que seu ambiente virtual (`venv`) está ativo.
2.  Execute o seguinte comando no terminal:
    ```bash
    python app_gui.py
    ```
3.  A janela da aplicação irá aparecer.
4.  Cole o link completo de um vídeo do YouTube no campo de texto.
5.  Clique no botão "Iniciar Processo".
6.  Aguarde enquanto o aplicativo baixa e processa o áudio. O status será exibido na janela.
7.  Ao final, uma mensagem de sucesso aparecerá e os arquivos `harmonia.wav` e `percussao.wav` estarão salvos em uma pasta chamada `resultado_separacao`.

